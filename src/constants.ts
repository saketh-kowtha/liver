import colors from 'colors'

export const title = colors.rainbow("LIVER")
export const helpDocs = `
${title} is a CLI tool

-> This will act as mock server as well as static server
-> Start your server 
-> You can also configure using .mockrc file
-> 2 static folders one is static folder which one can configure and next one is static assets under mocks folder
`

export const aboutDocs = `
${title} is a CLI tool
Developed by Saketh

Upcoming features:
-> Export and import
-> User interface to edit mock files
-> Controller support in mock files
`